name: Deploy Templatized Data Environment

on:
  pull_request:
    types: [closed]
    branches:
      - staging
      - main
  push:
    branches:
      - staging
      - main

jobs:
  deploy_data_environment:
    # Only run when PR is merged or on direct push to environment branches
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest

    env:
      # Read connection secret
      SNOWFLAKE_CONNECTIONS_SNOWFLAKE_DATA_ENGINEERING_2026_DEVOPS_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
      SNOWFLAKE_CONNECTIONS_SNOWFLAKE_DATA_ENGINEERING_2026_DEVOPS_USER: ${{ secrets.SNOWFLAKE_USER }}
      SNOWFLAKE_CONNECTIONS_SNOWFLAKE_DATA_ENGINEERING_2026_DEVOPS_AUTHENTICATOR: SNOWFLAKE_JWT
      SNOWFLAKE_CONNECTIONS_SNOWFLAKE_DATA_ENGINEERING_2026_DEVOPS_PRIVATE_KEY_FILE: ~/.snowflake/rsa_key.p8
      # SNOWFLAKE_CONNECTIONS_SNOWFLAKE_DATA_ENGINEERING_2026_DEVOPS_PASSWORD: ${{ secrets.SNOWFLAKE_DATA_ENGINEERING_2026_DEVOPS_PASSWORD }}

    steps:
      # Checkout step is necessary to access repository files
      - name: Checkout repository
        uses: actions/checkout@v4

      # Create RSA private key for Snowflake JWT authentication
      - name: Configure Snowflake RSA private key
        run: |
          mkdir -p ~/.snowflake
          echo "${{ secrets.SNOWFLAKE_PRIVATE_KEY }}" > ~/.snowflake/rsa_key.p8
          chmod 600 ~/.snowflake/rsa_key.p8

      # Determine environment based on target branch
      - name: Set environment
        id: set_env
        run: |
          TARGET_BRANCH="${GITHUB_BASE_REF:-${GITHUB_REF#refs/heads/}}"
          
          if [[ $TARGET_BRANCH == "staging" ]]; then
            echo "DEPLOY_ENV=staging" >> $GITHUB_ENV
            echo "Environment set to staging"
          elif [[ $TARGET_BRANCH == "main" ]]; then
            echo "DEPLOY_ENV=prod" >> $GITHUB_ENV
            echo "Environment set to production"
          else
            echo "Unexpected branch: $TARGET_BRANCH"
            exit 1
          fi

      # Install Snowflake CLI GitHub Action and point to config file
      - name: Install SnowflakeCLI
        uses: snowflakedb/snowflake-cli-action@v1.5
        with:
          cli-version: "latest"
          default-config-file-path: "config.toml"

      # Fetch latest changes from the repository to Snowflake
      - name: Fetch latest changes to Snowflake
        run: snow git fetch DEVOPS_DB.PUBLIC.MY_REPO

      # Deploy templates to the data environment -
      - name: Deploy templates to data environment
        run: |
          # Export TARGET_BRANCH for use in this step
          echo "Using branch: ${GITHUB_REF_NAME}"
          snow git execute @MY_REPO/branches/${GITHUB_REF_NAME}/module-1/hamburg_weather/pipeline/data/ \
          -D "env='${{ env.DEPLOY_ENV }}'" \
          --database=DEVOPS_DB \
          --schema=PUBLIC
          --name=MY_REPO